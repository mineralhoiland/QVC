"""
qvc.viz.publication — Publication-quality matplotlib figures for TEVC.

Generates PRL single-column (3.375 x 2.5 in) figures with:
- Colorblind-safe palette
- 8pt body / 9pt label fonts
- Vector (PDF) + raster (PNG @ 300 DPI) output
- Minimal visual noise (no gridlines, no chartjunk)
"""
